apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smtp2s3.fullname" . }}-test-connection"
  labels:
    {{- include "smtp2s3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smtp2s3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
